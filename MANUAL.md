# Hướng dẫn sử dụng Library 2D (Interactive Room)

Chào mừng bạn đến với plugin **Library 2D** - Giao diện thư viện tương tác phong cách căn phòng!

## 📸 1. Chuẩn bị ảnh nền
1. Tìm hoặc vẽ một bức ảnh căn phòng bạn yêu thích (định dạng `.png` hoặc `.jpg`).
2. Copy file ảnh vào thư mục cài đặt plugin:
   `Vault của bạn/.obsidian/plugins/loom-view/room-bg.png`
3. (Tùy chọn) Bạn có thể đổi tên ảnh trong phần **Settings** của plugin.

## ✨ 2. Tạo các vùng chọn tương tác (Hotspots)
Để căn phòng trở nên sống động, bạn cần gán các đồ vật trong ảnh với các thư mục ghi chú của mình:

1. Mở view **Library 2D** từ biểu tượng ruy băng (Ribbon) bên trái.
2. Nhấn nút **"✨ Vẽ vùng chọn mới"** ở góc trên bên phải ảnh.
3. **Nhấn và kéo chuột** trực tiếp lên ảnh để vẽ một khung vùng quanh món đồ bạn muốn (ví dụ: cái bàn, kệ sách, quả địa cầu).
4. Một cửa sổ hiện lên, hãy nhập:
   - **Tên vùng**: Tên gợi nhớ (ví dụ: Nhật ký, Sách kỹ năng).
   - **Đường dẫn Folder**: Tên thư mục trong Obsidian bạn muốn link tới (ví dụ: `journal` hoặc `work/projects`).
5. Nhấn **Lưu vùng chọn**.

## 📁 3. Duyệt File
- **Bấm vào đồ vật**: Một danh sách file "màu gỗ" sẽ hiện ra.
- **Duyệt thư mục con**: Nếu trong folder có nhiều thư mục nhỏ, bạn có thể bấm vào icon 📁 để đi sâu vào bên trong. Sử dụng **⬅️ QUAY LẠI** để trở ra.
- **Mở File**: Bấm vào bất kỳ file nào để mở nó trong một **Tab mới**. Căn phòng của bạn sẽ luôn được giữ nguyên trạng thái.
- **Sắp xếp**: File luôn được tự động xếp theo thời gian khởi tạo (Mới nhất lên đầu).

## 🛠️ 4. Quản lý Hotspots
Nếu bạn muốn xóa hoặc chỉnh sửa chi tiết các vùng chọn, hãy vào:
**Settings -> Loom View Settings -> Custom Hotspots**.

---
*Chúc bạn có những trải nghiệm ghi chú thật thú vị và trực quan!*
