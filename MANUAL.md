# Hướng dẫn sử dụng Library 2D (Interactive Room)

Chào mừng bạn đến với phiên bản mới của plugin **Library 2D** - Giờ đây đã hỗ trợ nhiều hồ sơ (Profiles)!

## 📸 1. Quản lý Hồ sơ (Profiles)
Tính năng mới cho phép bạn tạo nhiều "căn phòng" hoặc "khung cảnh" khác nhau.

1. Nhìn lên góc trên bên phải của giao diện Library 2D.
2. Bạn sẽ thấy một danh sách thả xuống (Dropdown).
3. Chọn **"➕ Thêm ảnh mới..."** để tạo một hồ sơ mới.
4. Đặt tên cho hồ sơ (ví dụ: "Phòng khách", "Bảo tàng").
5. Nhập tên file ảnh nền (ảnh phải nằm trong thư mục plugin, ví dụ `room-bg.png` hoặc `my-room.jpg`).

## ✨ 2. Tạo vùng chọn (Hotspots) cho từng Hồ sơ
Mỗi hồ sơ sẽ có các vùng chọn riêng biệt.

1. Chọn hồ sơ bạn muốn chỉnh sửa.
2. Nhấn nút **"✨ Vẽ vùng chọn mới"**.
3. Vẽ vùng chọn trên ảnh.
4. Nhập tên và đường dẫn thư mục như bình thường.
5. Nhấn **Lưu vùng chọn**.

## 📁 3. Duyệt File
- Chuyển đổi giữa các hồ sơ để xem các phòng khác nhau.
- Bấm vào vùng chọn để xem file.

## 🛠️ 4. Cài đặt
Bạn có thể quản lý (đổi tên, xóa) các hồ sơ trong phần **Settings** của plugin.
